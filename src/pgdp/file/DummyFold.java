package pgdp.file;

public class DummyFold {

    public static void main(String[] args) {
        //This "implementation" of Fold does absolutely nothing!
        //So it should fail the tests, of course.
        //You can import other implementations of Fold in this package (pgdp.file),
        //and set the "fold" attribute in FoldTest to have it tested.
        //You do not need to and shouldn't submit these different implementations of fold to artemis,
        //the artemis tests will set and change the "fold" attribute in FoldTest itself!
    }
}
